import React from "react";
import Profile from "./Profile"

import "./NavBar.css";

const NavBar = () => {
  return (
    <nav className="navbar">
      <Profile />
    </nav>
  );
};

export default NavBar;
